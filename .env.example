# Obligatorios
SESSION_SECRET=cambia-esto-por-32-o-mas-caracteres-aleatorios
PILOT_PIN=4321
# Opcional en local (sin esto se usa PGlite en data/pglite). Obligatorio en Vercel (Neon).
# DATABASE_URL=postgresql://user:pass@host/db?sslmode=require
PORT=3000
PILOT_PIN_SHARED=true   # solo en local; en producción déjalo fuera (apagado)
# Clave de la página /super (24 caracteres o más). Sin ella, /super no abre.
# SUPER_KEY=una-clave-larga-de-24-o-mas-caracteres
# Modo demo (activo por defecto): reset_demo, seed_load y el teléfono del simulador. Ponlo en false con datos reales.
# DEMO_MODE=false

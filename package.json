{
  "name": "iae-salidas-connected-pilot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "test": "node --test",
    "check": "node --check server/index.js && node --check connected-bridge.js && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "optionalDependencies": {
    "whatsapp-web.js": "^1.34.6"
  },
  "dependencies": {
    "pg": "^8.16.3"
  }
}
